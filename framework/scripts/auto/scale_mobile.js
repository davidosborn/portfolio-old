/**
 * If the user is running a mobile browser, check their screen resolution and
 * scale the page to make it more usable.
 *
 * @note This script expects to be executed at the end of the body.
 */
/*(function()
{
	if (MOBILE === true)
	{
		document.body.style.fontSize = '1.5em';
	}
})();*/
